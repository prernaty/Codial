<!--<header class="main-header">
    Page Header

    <ul>
        <% if (locals.user) { %>
            <li>
                <%= user.name %>
            </li>
            <li>
                <a href="/users/sign-out">Sign Out</a>
            </li>
        <% } else { %>
            <li>
                <a href="/users/sign-in">Sign In</a>
            </li>
            <li>
                <a href="/users/sign-up">Sign Up</a>
            </li>
        <% } %>
    </ul>
</header> -->
    <link rel="stylesheet" href="/css/header.css">
    <div class="container">
        <h1 class="mh-logo">
            <img src="http://flexbox.ninja/assets/images/logo.svg" width="170" height="50" alt="Flexbox.ninja">
        </h1>
        <nav class="main-nav">
            <ul class="main-nav-list">
                <% if (locals.user) { %>
                    <li>
                        <a href="/users/profile"><%= user.name %></a>
                    </li>
                    <li>
                        <a href="/users/sign-out">Sign Out</a>
                    </li>
                    <% } else { %>
                        <li>
                            <a href="/users/sign-in">Sign In</a>
                        </li>
                        <li>
                            <a href="/users/sign-up">Sign Up</a>
                        </li>
                    <% } %>
            </ul>
        </nav>
    </div>